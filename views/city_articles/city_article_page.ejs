<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>[<%= article.type %>] <%= article.title %> | TaiwanMe</title>
    <link rel="icon" href="/image/logo.png" type="image/png">
    
    <link rel="stylesheet" href="/style.css">

    <style>
        /* =========================================
           文章內頁專屬樣式 (繼承自 style.css)
           ========================================= */

        /* 1. 沉浸式 Hero Header */
        .article-hero {
            width: 100%;
            height: 60vh;
            min-height: 400px;
            /* 動態讀取文章封面圖 */
            background-image: url('<%= article.heroImage %>');
            background-size: cover;
            background-position: center;
            position: relative;
        }

        .article-hero::after {
            content: '';
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0,0,0,0.3); /* 增加一層暗色濾鏡讓文字更清楚 */
        }

        .hero-content {
            position: absolute;
            bottom: 60px;
            left: 50%;
            transform: translateX(-50%);
            width: 90%;
            max-width: 900px;
            text-align: center;
            color: #fff;
            z-index: 2;
            text-shadow: 0 2px 10px rgba(0,0,0,0.5);
        }

        .hero-tags {
            margin-bottom: 15px;
        }
        .hero-tag {
            background: var(--accent-color);
            color: #fff;
            padding: 5px 12px;
            font-size: 0.85rem;
            font-weight: bold;
            border-radius: 4px;
            margin: 0 5px;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-family: var(--font-eng);
        }

        .hero-title {
            font-family: var(--font-eng);
            font-size: 3rem;
            font-weight: 900;
            line-height: 1.2;
            margin: 0 0 10px;
        }
        
        .hero-subtitle {
            font-size: 1.2rem;
            font-weight: normal;
            opacity: 0.9;
            font-family: var(--font-serif);
        }

        /* 2. 文章內容容器 */
        .content-container {
            max-width: 800px;
            margin: 60px auto 100px;
            padding: 0 20px;
        }

        /* 簡介文字 */
        .intro-text {
            font-size: 1.15rem;
            line-height: 1.8;
            color: #444;
            text-align: justify;
            margin-bottom: 40px;
            padding-bottom: 30px;
            border-bottom: 1px solid #eee;
            font-family: var(--font-main);
        }

        /* 交通資訊卡 */
        .transport-card {
            background-color: #f8f9fa;
            border-left: 5px solid var(--accent-color);
            padding: 25px;
            border-radius: 0 8px 8px 0;
            margin-bottom: 50px;
        }
        .transport-title {
            font-family: var(--font-eng);
            font-size: 1.2rem;
            font-weight: 700;
            color: #333;
            margin-bottom: 10px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .transport-info {
            font-size: 1rem;
            color: #555;
            line-height: 1.6;
            font-family: var(--font-main);
        }

        /* 3. 景點時間軸樣式 */
        .itinerary-section-title {
            text-align: center;
            font-family: var(--font-eng);
            font-size: 1.8rem;
            margin-bottom: 40px;
            color: #333;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .timeline {
            position: relative;
            padding-left: 30px;
        }
        .timeline::before {
            content: '';
            position: absolute;
            left: 0;
            top: 10px;
            bottom: 0;
            width: 2px;
            background: #e0e0e0;
        }

        .spot-card {
            position: relative;
            margin-bottom: 50px;
        }
        
        /* 時間點圓圈 */
        .spot-dot {
            position: absolute;
            left: -36px;
            top: 5px;
            width: 14px;
            height: 14px;
            background: var(--accent-color);
            border-radius: 50%;
            border: 3px solid #fff;
            box-shadow: 0 0 0 2px var(--accent-color);
        }

        .spot-time {
            font-family: var(--font-eng);
            font-weight: bold;
            color: var(--accent-color);
            font-size: 0.9rem;
            margin-bottom: 5px;
            text-transform: uppercase;
        }

        .spot-name {
            font-size: 1.5rem;
            font-weight: 700;
            margin: 0 0 5px;
            color: #222;
            font-family: var(--font-main);
        }

        .spot-address {
            font-size: 0.9rem;
            color: #888;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 5px;
            font-family: var(--font-main);
        }

        .spot-desc {
            font-size: 1rem;
            color: #555;
            line-height: 1.7;
            background: #fff;
            font-family: var(--font-main);
        }

        /* Google Map 按鈕 */
        .btn-map-link {
            display: inline-block;
            background: #fff;
            border: 1px solid #ddd;
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 0.8rem;
            color: #555;
            text-decoration: none;
            transition: 0.3s;
            margin-left: 10px;
            vertical-align: middle;
            font-family: var(--font-eng);
        }
        .btn-map-link:hover {
            background: #4285F4;
            color: #fff;
            border-color: #4285F4;
        }

        /* 返回按鈕 */
        .back-nav {
            text-align: center;
            margin-top: 60px;
            padding-top: 40px;
            border-top: 1px solid #eee;
        }
        .btn-back {
            display: inline-block;
            text-decoration: none;
            color: #333;
            border: 1px solid #333;
            padding: 12px 35px;
            border-radius: 30px;
            font-weight: bold;
            font-family: var(--font-eng);
            transition: 0.3s;
            /* 讓文字首字母大寫，看起來更專業 */
            text-transform: capitalize; 
        }
        .btn-back:hover {
            background: #333;
            color: #fff;
        }
        
        /* 手機版調整 */
        @media (max-width: 768px) {
            .hero-title { font-size: 2rem; }
            .content-container { padding: 0 20px; }
        }
    </style>
</head>
<body>

    <%- include('../partials/header') %>

    <header class="article-hero">
        <div class="hero-content">
            <div class="hero-tags">
                <% article.tags.forEach(function(tag){ %>
                    <span class="hero-tag"><%= tag %></span>
                <% }); %>
            </div>
            
            <h1 class="hero-title">
                [<%= article.type %>] <%= article.title %>
            </h1>
            
            <h2 class="hero-subtitle"><%= article.subTitle %></h2>
        </div>
    </header>

    <main class="content-container">
        
        <div class="intro-text">
            <p><%= article.intro %></p>
        </div>

        <div class="transport-card">
            <div class="transport-title">🚆 Transportation Guide</div>
            <div class="transport-info">
                <%- article.transportation %> 
            </div>
        </div>

        <h3 class="itinerary-section-title">Itinerary Highlights</h3>
        
        <div class="timeline">
            <% article.spots.forEach(function(spot) { %>
                <div class="spot-card">
                    <div class="spot-dot"></div> <div class="spot-time"><%= spot.time %></div>
                    
                    <h4 class="spot-name">
                        <%= spot.name %>
                    </h4>
                    
                    <div class="spot-address">
                        📍 <%= spot.address %>
                        <a href="<%= spot.mapLink %>" target="_blank" class="btn-map-link">View on Google Map ↗</a>
                    </div>
                    
                    <div class="spot-desc">
                        <%= spot.description %>
                    </div>
                </div>
            <% }); %>
        </div>

        <div class="back-nav">
            <a href="/search_by_city/<%= citySlug %>" class="btn-back">
                ← Back to <%= citySlug.replace('_', ' ') %> Guide
            </a>
        </div>

    </main>

    <%- include('../partials/footer') %>
    
    <script src="/script.js"></script>

</body>
</html>