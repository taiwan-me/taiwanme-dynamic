<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= article.title %> | TaiwanMe Entertainment</title>
    <link rel="icon" href="/image/logo.png" type="image/png">
    <link rel="stylesheet" href="/style.css">
    <style>
        /* ========================================= */
        /* 1. Page Header (極簡純文字風格)           */
        /* ========================================= */
        .page-header-text {
            width: 100%;
            padding: 100px 20px 80px;
            background-color: #f9f9f9;
            border-bottom: 1px solid #eee; 
            text-align: center;
            margin-bottom: 60px;
        }

        .header-content {
            max-width: 800px;
            margin: 0 auto;
        }

        .header-tag {
            display: inline-block;
            font-size: 0.8rem;
            font-weight: 700;
            color: var(--accent-color); 
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 20px;
            padding: 5px 15px;
            border: 1px solid var(--accent-color);
            border-radius: 30px;
        }

        .header-title {
            font-family: var(--font-serif);
            font-size: 3.5rem; 
            color: #222;
            margin: 0 0 25px; 
            line-height: 1.2;
            font-weight: 800;
        }
        
        .header-intro {
            font-family: var(--font-main);
            font-size: 1.2rem;
            color: #555;
            line-height: 1.8;
            font-weight: 400;
            opacity: 0.9;
        }

        /* ========================================= */
        /* 2. Filter Section (縣市篩選)              */
        /* ========================================= */
        .filter-container {
            max-width: 1200px;
            margin: 0 auto 50px;
            padding: 0 20px;
            display: flex;
            align-items: center;
            justify-content: flex-start;
            gap: 20px;
        }

        .filter-label {
            font-size: 0.9rem;
            font-weight: 800;
            color: #999;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .city-select {
            padding: 12px 24px;
            font-size: 1rem;
            border: 1px solid #e0e0e0;
            background-color: #fff;
            color: #333;
            min-width: 260px;
            cursor: pointer;
            border-radius: 8px; 
            outline: none;
            font-weight: 600;
            transition: all 0.3s ease;
            box-shadow: 0 2px 5px rgba(0,0,0,0.03);
        }

        .city-select:hover, .city-select:focus {
            border-color: var(--accent-color);
            box-shadow: 0 4px 10px rgba(0,0,0,0.08);
        }

        /* ========================================= */
        /* 3. Info Cards Grid (純文字資訊卡)         */
        /* ========================================= */
        .grid-container {
            max-width: 1200px;
            margin: 0 auto 120px;
            padding: 0 20px;
            display: grid;
            grid-template-columns: repeat(3, 1fr); /* 3欄排版，閱讀舒適 */
            gap: 30px;
        }

        .info-card {
            display: flex;
            flex-direction: column;
            background: #fff;
            border: 1px solid #eee;
            border-radius: 8px;
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            height: 100%;
            position: relative;
            /* 頂部裝飾線 */
            border-top: 4px solid var(--accent-color);
        }

        .info-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(0,0,0,0.08);
            border-color: #ddd;
        }

        .info-card.hidden { display: none; }

        .card-body {
            padding: 30px;
            text-align: left;
            display: flex;
            flex-direction: column;
            flex-grow: 1;
        }

        /* 城市標籤 */
        .card-meta {
            margin-bottom: 15px;
        }
        
        .card-city-tag {
            display: inline-block;
            font-size: 0.75rem;
            color: #555;
            background-color: #f4f4f4;
            padding: 4px 10px;
            border-radius: 4px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        /* 標題連結 */
        .card-title-link {
            text-decoration: none;
            color: #222;
            transition: color 0.2s;
        }
        .card-title-link:hover { color: var(--accent-color); }

        .card-title {
            font-size: 1.4rem;
            font-weight: 700;
            margin: 0 0 15px;
            line-height: 1.3;
            font-family: var(--font-serif);
        }

        .card-desc {
            font-size: 0.95rem;
            color: #666;
            line-height: 1.7;
            margin-bottom: 30px;
            flex-grow: 1; 
        }
        
        /* ✅ 底部連結區 (修改為純文字連結) */
        .card-footer {
            margin-top: auto;
            display: flex;
            align-items: center;
            justify-content: flex-start; /* 靠左排列 */
            gap: 20px; /* 連結之間的間距 */
            padding-top: 20px;
            border-top: 1px dashed #eee;
        }

        .text-link {
            font-size: 0.85rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            text-decoration: none;
            color: #888;
            display: flex;
            align-items: center;
            gap: 5px;
            transition: all 0.3s;
        }

        /* 不同的連結顏色 Hover 效果 */
        .text-link.website:hover { color: var(--accent-color); }
        .text-link.map:hover { color: #2a9d8f; /* 地圖用綠色或不同顏色區分 */ }

        .link-icon { font-size: 1rem; }

        /* RWD */
        @media (max-width: 1024px) {
            .grid-container { grid-template-columns: repeat(2, 1fr); }
        }
        @media (max-width: 768px) {
            .page-header-text { padding: 60px 20px 40px; }
            .header-title { font-size: 2.5rem; }
            
            .grid-container { grid-template-columns: 1fr; gap: 20px; }
            
            .filter-container { flex-direction: column; align-items: stretch; gap: 10px; }
            .city-select { width: 100%; }
        }
    </style>
</head>
<body>
    <%- include('../partials/header') %>

    <div class="page-header-text">
        <div class="header-content">
            <span class="header-tag">Directory</span>
            <h1 class="header-title"><%= article.title %></h1>
            <% if (article.intro) { %>
                <p class="header-intro"><%= article.intro %></p>
            <% } %>
        </div>
    </div>

    <div class="filter-container">
        <span class="filter-label">FILTER BY CITY</span>
        <select id="cityFilter" class="city-select">
            <option value="all">View All Locations (全台灣)</option>
        </select>
    </div>

    <div class="grid-container" id="gridContainer">
        <% if (article.spots && article.spots.length > 0) { %>
            <% article.spots.forEach(spot => { %>
                <div class="info-card" data-city="<%= spot.city %>">
                    <div class="card-body">
                        <div class="card-meta">
                            <span class="card-city-tag">📍 <%= spot.city %></span>
                        </div>
                        
                        <a href="<%= spot.link || '#' %>" target="_blank" class="card-title-link">
                            <h3 class="card-title"><%= spot.name %></h3>
                        </a>
                        
                        <p class="card-desc"><%= spot.description %></p>
                        
                        <div class="card-footer">
                            <a href="<%= spot.link || '#' %>" target="_blank" class="text-link website">
                                <span class="link-text">Official Website</span> ↗
                            </a>
                            
                            <% if (spot.mapLink) { %>
                                <a href="<%= spot.mapLink %>" target="_blank" class="text-link map">
                                    <span class="link-text">Google Map</span> 📍
                                </a>
                            <% } %>
                        </div>
                    </div>
                </div>
            <% }) %>
        <% } else { %>
            <div style="grid-column: 1/-1; text-align: center; color: #999; padding: 80px;">
                <h3>Content Coming Soon...</h3>
                <p>We are currently curating the best spots for you.</p>
            </div>
        <% } %>
    </div>

    <%- include('../partials/footer') %>
    <script src="/script.js"></script>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const cityFilter = document.getElementById('cityFilter');
            const cards = document.querySelectorAll('.info-card');

            // 1. 全台灣縣市列表
            const allCities = [
                "Keelung", "Taipei", "New Taipei", "Taoyuan", "Hsinchu", "Miaoli", 
                "Taichung", "Changhua", "Nantou", "Yunlin", "Chiayi",
                "Tainan", "Kaohsiung", "Pingtung", 
                "Yilan", "Hualien", "Taitung", 
                "Penghu", "Kinmen", "Matsu"
            ];

            // 2. 生成選單
            allCities.forEach(city => {
                const option = document.createElement('option');
                option.value = city;
                option.textContent = city;
                cityFilter.appendChild(option);
            });

            // 3. 篩選邏輯
            cityFilter.addEventListener('change', (e) => {
                const selectedCity = e.target.value;

                cards.forEach(card => {
                    const cardCity = card.getAttribute('data-city');
                    
                    if (selectedCity === 'all') {
                        card.classList.remove('hidden');
                    } else {
                        if (cardCity && (cardCity.includes(selectedCity) || selectedCity.includes(cardCity))) {
                            card.classList.remove('hidden');
                        } else {
                            card.classList.add('hidden');
                        }
                    }
                });
            });
        });
    </script>
</body>
</html>