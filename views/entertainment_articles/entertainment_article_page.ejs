<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= article.title %> | TaiwanMe Entertainment</title>
    <link rel="icon" href="/image/logo.png" type="image/png">
    <link rel="stylesheet" href="/style.css">
    <style>
        /* ========================================= */
        /* 1. Article Hero Banner */
        /* ========================================= */
        .article-hero {
            background-image: url('<%= article.heroImage %>');
            width: 100%; 
            height: 60vh; 
            min-height: 400px;
            background-size: cover; 
            background-position: center;
            position: relative; 
            display: flex; 
            align-items: flex-end; /* 文字置底 */
            margin-bottom: 60px;
        }

        /* 漸層遮罩，讓文字更清晰 */
        .article-hero::after {
            content: ''; 
            position: absolute; 
            top: 0; left: 0; 
            width: 100%; height: 100%;
            background: linear-gradient(to bottom, rgba(0,0,0,0.1) 0%, rgba(0,0,0,0.8) 100%);
        }

        .hero-text-content {
            position: relative; 
            z-index: 2; 
            color: #fff; 
            padding: 0 5% 60px; 
            max-width: 1200px; 
            margin: 0 auto; 
            width: 100%;
        }

        .article-tag-row {
            margin-bottom: 15px;
        }

        .hero-tag {
            background-color: var(--accent-color);
            color: #fff;
            padding: 5px 12px;
            font-size: 0.85rem;
            font-weight: bold;
            border-radius: 4px;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-right: 8px;
        }

        .article-title { 
            font-size: 3rem; 
            font-weight: 800; 
            margin: 0 0 15px; 
            line-height: 1.2; 
            font-family: serif;
            text-shadow: 0 2px 10px rgba(0,0,0,0.3);
        }

        .article-intro { 
            font-size: 1.2rem; 
            line-height: 1.6; 
            opacity: 0.95; 
            max-width: 800px; 
            font-weight: 500;
        }
        
        /* ========================================= */
        /* 2. Spots List (交錯式排版) */
        /* ========================================= */
        .spots-container { 
            max-width: 1000px; 
            margin: 0 auto 100px; 
            padding: 0 20px; 
        }
        
        .ent-spot-card {
            display: flex; 
            gap: 40px; 
            margin-bottom: 80px; 
            align-items: center;
        }

        /* 偶數項目反轉排列 (圖右文左) */
        .ent-spot-card:nth-child(even) { 
            flex-direction: row-reverse; 
        }
        
        .spot-img-box { 
            flex: 1; 
            height: 350px; 
            overflow: hidden; 
            border-radius: 12px; 
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            position: relative;
        }

        .spot-img { 
            width: 100%; 
            height: 100%; 
            object-fit: cover; 
            transition: transform 0.6s ease; 
        }
        
        .ent-spot-card:hover .spot-img { 
            transform: scale(1.05); 
        }
        
        .spot-info { 
            flex: 1; 
            padding: 10px; 
        }

        .spot-location { 
            color: var(--accent-color); 
            font-weight: 800; 
            text-transform: uppercase; 
            letter-spacing: 2px; 
            font-size: 0.9rem; 
            margin-bottom: 10px; 
            display: block; 
            font-family: sans-serif;
        }

        .spot-name { 
            font-size: 2rem; 
            font-weight: 700; 
            color: #222; 
            margin: 0 0 20px; 
            font-family: serif;
            line-height: 1.3;
        }

        .spot-desc { 
            color: #555; 
            line-height: 1.8; 
            font-size: 1.05rem; 
            text-align: justify;
        }

        /* 手機版響應式 */
        @media (max-width: 768px) {
            .article-hero { height: 50vh; }
            .article-title { font-size: 2.2rem; }
            
            .ent-spot-card, .ent-spot-card:nth-child(even) { 
                flex-direction: column; 
                gap: 20px;
                margin-bottom: 60px;
            }
            
            .spot-img-box { 
                width: 100%; 
                height: 250px; 
            }

            .spot-name { font-size: 1.6rem; }
        }
    </style>
</head>
<body>
    <%- include('../partials/header') %>

    <div class="article-hero">
        <div class="hero-text-content">
            <div class="article-tag-row">
                <% if (article.tags) { %>
                    <% article.tags.forEach(tag => { %>
                        <span class="hero-tag"><%= tag %></span>
                    <% }) %>
                <% } %>
            </div>
            <h1 class="article-title"><%= article.title %></h1>
            <p class="article-intro"><%= article.intro %></p>
        </div>
    </div>

    <div class="spots-container">
        <% if (article.spots && article.spots.length > 0) { %>
            <% article.spots.forEach(spot => { %>
                <div class="ent-spot-card">
                    <div class="spot-img-box">
                        <img src="<%= spot.image %>" alt="<%= spot.name %>" class="spot-img" onerror="this.src='https://placehold.co/600x400/eee/333?text=No+Image'">
                    </div>
                    <div class="spot-info">
                        <span class="spot-location">
                            📍 <%= spot.city %>
                        </span>
                        <h2 class="spot-name"><%= spot.name %></h2>
                        <p class="spot-desc"><%= spot.description %></p>
                    </div>
                </div>
            <% }) %>
        <% } else { %>
            <div style="text-align:center; color:#999; padding:50px;">
                <h3>Content coming soon...</h3>
            </div>
        <% } %>
    </div>

    <%- include('../partials/footer') %>
    <script src="/script.js"></script>
</body>
</html>